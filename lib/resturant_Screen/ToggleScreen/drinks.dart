import 'package:flutter/material.dart';

import '../componnent/componnent.dart';

class drinks extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return ArticleBuilder(context);
  }

}